% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixchisq.R
\name{dmixchisq}
\alias{dmixchisq}
\title{Density for a finite mixture of chi-square distributions}
\usage{
dmixchisq(x, df, w, log = FALSE)
}
\arguments{
\item{x}{Vector of quantiles.}

\item{df}{Numeric vector of degrees of freedom. The value \code{0} is allowed and
is treated as a point mass at zero.}

\item{w}{Numeric vector of mixture weights.}

\item{log}{Logical; if \code{TRUE}, returns the log-density.}
}
\value{
Numeric vector of density values.
}
\description{
Computes the density of a finite mixture of chi-square distributions. The
special case \code{df = 0} is interpreted as a point mass at zero, so the returned
value describes only the continuous density. At \code{x = 0}, the function returns
\code{Inf} when there is atom at zero and \code{log = FALSE}, and \code{Inf} on the log scale
when \code{log = TRUE}.
}
\examples{
dmixchisq(c(0, 1, 2), df = c(0, 1, 3), w = c(0.5, 0.25, 0.25))
}
